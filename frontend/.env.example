# Base URL of the backend API. Leave empty in dev (Vite proxy handles it).
# In prod (Vercel), set this to your Render/Railway backend URL, e.g.:
#   VITE_API_BASE=https://stock-api.onrender.com
VITE_API_BASE=
