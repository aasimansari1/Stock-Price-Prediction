# Comma-separated list of allowed CORS origins, or * for any.
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Port the FastAPI server listens on. Render/Railway inject this automatically.
PORT=8000
